import { WorkspacePlaceholder } from "@/components/workspace-placeholder";

export default function ApplicationsPage() {
  return <WorkspacePlaceholder title="Universities" subtitle="Manage target universities." djangoPath="/applications/" />;
}
