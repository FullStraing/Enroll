import { WorkspacePlaceholder } from "@/components/workspace-placeholder";

export default function SettingsPage() {
  return <WorkspacePlaceholder title="Settings" subtitle="Profile and security." djangoPath="/settings/" />;
}
