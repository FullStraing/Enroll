import { WorkspacePlaceholder } from "@/components/workspace-placeholder";

export default function TasksPage() {
  return <WorkspacePlaceholder title="Tasks" subtitle="Track admissions tasks." djangoPath="/tasks/" />;
}
